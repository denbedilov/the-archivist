app = "the-archivist"

[build]
  image = "Dockerfile"

[env]
  # Не указываем тут токен, лучше добавить через команду fly secrets set BOT_TOKEN=твой_токен
  # но если хочешь, можно прописать напрямую:
  # BOT_TOKEN = "твой_токен_сюда"

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [[services.ports]]
    port = 8080
    handlers = ["http"]
